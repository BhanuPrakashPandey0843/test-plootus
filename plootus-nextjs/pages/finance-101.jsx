import Blogs from './blogs';

/**
 * Both /blogs and /finance101 render the same component and logic.
 * This ensures consistency and satisfies the requirement for both routes.
 */
export default Blogs;
